name: "Prettier"

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

jobs:
  Deploy:
    runs-on: "ubuntu-latest"

    steps:
      - name: "Checkout"
        uses: "actions/checkout@v4"

      - name: "Check prettier"
        run: "npm exec -- prettier --check ."
